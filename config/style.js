const baseline = 12

const color = {
  primary: {
    main: 'rgb(0,130,220)',
    contrast: 'white'
  },
  grey: {
    dark: '#222'
  }
}

const font = {
  family: 'sans-serif',
  size: {
    small: 14,
    medium: 18,
    large: 30
  }
}

const grid = {
  baseline,
  columns: 12,
  gutter: 40
}

export { color, font, grid }
